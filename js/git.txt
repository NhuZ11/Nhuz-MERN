// initial one time
git init 
git remote add origin git repo 
        eg: git remote add origin https://github.com/NhuZ11/Nhuz-MERN.git
        2.1 to check number  (optional): git remote -v

//daily use 

git status (optional)

git add .    or git add fileName(Eg: git add index.html)
git commit -m "html-css-jss"
git push     (git push --set-upstream origin your-branch-name //at start)


git clone url   // for clone others code


//for git pull 
git pull (must be inside the clone file)